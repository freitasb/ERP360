services:
  rabbitmq:
    image: rabbitmq:3-management
    container_name: erp360-rabbitmq
    ports:
      - "5672:5672"     # AMQP (app conecta aqui)
      - "15672:15672"   # painel web
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
